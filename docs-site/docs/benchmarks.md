---
sidebar_position: 3
---

# Benchmarks

Generated by `npm run bench:report` — the full suite (`test/bench/`) in one run, every comparison row from the same process. Snapshot details:

- **Date**: 2026-09-09
- **Machine**: bby.fedora (AMD Ryzen 7 8745HS w/ Radeon 780M Graphics)
- **Node**: v24.19.0

## Results

| Scenario | Mean | Throughput | RME |
|---|---|---|---|
| getValues - 100 fields, depth 3 — legacy: chained set per key | 96µs | 10,403/s | 0.3% |
| getValues - 100 fields, depth 3 — setOwned: copy-on-write merge | 59µs | 17,080/s | 0.3% |
| isDirty scan - same form — isDirty full scan | 40µs | 25,173/s | 0.3% |
| single field change - 100 controlled inputs mounted — f0rm Field: change re-renders 1 of 100 | 171µs | 5,849/s | 0.8% |
| single field change - 100 controlled inputs mounted — react-hook-form Controller: change 1 of 100 | 158µs | 6,319/s | 0.5% |
| single field change - 100 controlled inputs mounted — tanstack-react-form field: change 1 of 100 | 365µs | 2,737/s | 0.6% |
| single field change - 100 controlled inputs mounted — f0rm Field uncontrolled: change 1 of 100 | 14µs | 72,605/s | 0.2% |
| single field change - 100 controlled inputs mounted — react-hook-form register (uncontrolled): change 1 of 100 | 13µs | 79,520/s | 0.2% |
| submit path - 100 sync validators — getValues + validate | 8µs | 121,207/s | 0.2% |
| 1000-field controlled form - single field change — f0rm Field: change re-renders 1 of 1000 | 890µs | 1,124/s | 0.4% |
| 1000-field controlled form - single field change — react-hook-form Controller: change 1 of 1000 | 1373µs | 728/s | 0.6% |
| async validation storm - 50 debounced async validators — burst: 3 changes x 50 fields, settle via trigger (1 run per field) | 25.9ms | 39/s | 1.5% |
| full trigger wait - 100 mixed validators — await trigger(form): 50 sync + 50 async validators settle | 5816µs | 172/s | 2.6% |

## CI regression bounds

The same suite runs in CI (`.github/workflows/ci.yml`, `bench` job) with
`BENCH_ASSERT=1`, which activates per-row bounds inline in the bench files.
The bounds carry ~5–8× headroom over local means deliberately: shared GitHub
runners are noisy, and the job exists to catch order-of-magnitude
regressions, not to race. Bounds (ms):

| Bound (ms) | Guards |
|---|---|
| 1.0 | f0rm `Field`: change re-renders 1 of 100 |
| 0.1 | f0rm `Field uncontrolled`: change 1 of 100; `getValues + validate` submit path |
| 5.0 | f0rm `Field`: change re-renders 1 of 1000 |
| 150 | async validation storm (3 changes × 50 debounced validators) |
| 40 | `await trigger(form)`: 50 sync + 50 async validators |
| 0.4 | `getValues` copy-on-write ownership merge |
| 0.3 | `isDirty` full scan |

## Reading the numbers

- The re-render count is asserted inline by the benches themselves: the
  100/1000-field rows throw if more than 1 of N fields re-renders per
  change, and the storm row throws unless debounce coalesces the 150-event
  burst into exactly 50 validator runs.
- Runs wobble between executions (µs-scale rows flip ±10% run to run) —
  compare ratios, not absolute values across snapshots.
- Competitor rows (react-hook-form, tanstack-react-form) run in the same
  process with the same tree shape; they have no CI bounds.

## Reproduce

```sh
npm run bench:report           # full suite -> regenerates this page
npx vitest bench --run test/bench/   # interactive table only
```
